[package]
name = "identity"
version = "0.0.0"
edition = "2018"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
hotg-rune-core = { version = "^0.9.0", default-features = false }
hotg-rune-proc-blocks = "^0.9.0"

[workspace]

[package.metadata.release]
release = false
