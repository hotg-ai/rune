[workspace]
members = [
    "codegen",
    "ffi",
    "images/*",
    "proc_blocks/*",
    "rune",
    "runic-types",
    "runtime",
    "syntax",
    "xtask",
    "wasmer-runtime",
]
exclude = [ "examples/*" ]

[profile.release]
debug = true
