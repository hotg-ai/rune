[workspace]
members = [
    "codegen",
    "ffi",
    "images/*",
    "proc_blocks/*",
    "rune",
    "runic-types",
    "runtime",
    "syntax",
    "xtask",
    "wasmer-runtime",
    "wasmer-runtime2",
]
exclude = [ "examples/*" ]

[profile.release]
debug = true
