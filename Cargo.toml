[workspace]
members = [
    "codegen",
    "ffi",
    "images/*",
    "proc_blocks/*",
    "rune",
    "runic-types",
    "runtime",
    "syntax",
    "xtask",
    "wasmer-runtime",
]
exclude = [ "examples/*", "proc_blocks/python" ]

[profile.release]
debug = true

